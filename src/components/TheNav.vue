<!-- <template>
  <nav class="nav">
    <div class="nav-wrap">
      <div class="logo" @click="$emit('goto','hero')">
        <span class="logo-txt">{{ t.brand }}</span>
      </div>

      <div class="nav-links">
        <a v-for="l in navItems" :key="l.id" class="nlink" @click="$emit('goto',l.id)">
          {{ l.label }}
          <span class="nlink-line"></span>
        </a>
      </div>

      <button class="tbtn" @click="$emit('toggleTheme')">🌙</button>
    </div>
  </nav>
</template>

<script>
export default{
  props:{ t:Object, navItems:Array }
}
</script> -->
